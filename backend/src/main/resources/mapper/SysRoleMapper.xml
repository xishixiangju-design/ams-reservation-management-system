<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trae.ams.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.trae.ams.entity.SysRole">
        <id column="id" property="id" />
        <result column="code" property="code" />
        <result column="name" property="name" />
    </resultMap>

    <insert id="insert" parameterType="com.trae.ams.entity.SysRole" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (code, name)
        VALUES (#{code}, #{name})
    </insert>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT id, code, name
        FROM sys_role
        WHERE code = #{code}
    </select>

</mapper>
