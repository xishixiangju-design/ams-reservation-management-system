<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trae.ams.mapper.AmsMemberLevelLogMapper">
    <insert id="insert" parameterType="com.trae.ams.entity.AmsMemberLevelLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ams_member_level_log (
            user_id, old_level, new_level, trigger_amount, total_consumption, create_time
        ) VALUES (
            #{userId}, #{oldLevel}, #{newLevel}, #{triggerAmount}, #{totalConsumption}, NOW()
        )
    </insert>
</mapper>
